
import 'package:flutter/material.dart';

class AppPaddings {
  
  /// General padding for the majority of your screens.
  static const EdgeInsets all16 = EdgeInsets.all(16);

  /// A smaller padding, e.g. for tighter spaces.
  static const EdgeInsets all8 = EdgeInsets.all(8);

  /// Padding specifically for buttons.
  static const EdgeInsets buttonPadding = EdgeInsets.symmetric(
    horizontal: 24,
    vertical: 12,
  );

}
